# Change Log

## [0.0.1] — Unreleased

- Syntax highlighting for `.knot` files (keywords, types, literals, operators, comments, decorators)
- Language Server Protocol client (diagnostics, completion, hover)
- Language configuration (bracket matching, auto-closing, comment toggling)
- Configuration setting `knot.lsp.path` to customize compiler binary location
